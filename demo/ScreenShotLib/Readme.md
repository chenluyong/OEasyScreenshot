# 这是ScreenShotLibDemo的简要说明文档

在没有改动的情况下，这个项目将在根目录 bin 文件夹下生成 ScreenShotLibDemo.exe。

很显然，他是一个示例程序，它将动态加载一个名为 OEScreenshot.dll 的动态库。

所以，为了成功运行这个demo，首先要生成这个动态库。

在当前版本中，这个动态库由项目 OEScreenshotLib.pro 来生成构建。

我们可以在根目录的 src 文件夹中轻松的找到它。